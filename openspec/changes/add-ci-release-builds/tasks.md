## 1. Implementation
- [x] 1.1 Add a GitHub Actions workflow that runs on pushes to `master` and builds Tauri release artifacts.
- [x] 1.2 Upload platform-specific build outputs as workflow artifacts.
- [x] 1.3 Validate the change with `openspec validate add-ci-release-builds --strict`.
