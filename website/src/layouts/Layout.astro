---
interface Props {
  title: string;
  description?: string;
}
const { title, description = 'A desktop overlay that visualizes split and ergonomic keyboard layouts.' } = Astro.props;
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <title>{title} — Keyboard Helper</title>
    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-TVX77XQM39"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-TVX77XQM39');
    </script>
    <style>
      :root {
        --bg: #0d1120;
        --bg2: #141929;
        --accent: #3cc9c7;
        --text: #e4e7ef;
        --text-muted: #8b91a8;
        --border: rgba(255,255,255,0.08);
        --key-bg: linear-gradient(155deg, #20263b, #0f1325);
      }
      :global(*), :global(*::before), :global(*::after) { box-sizing: border-box; margin: 0; padding: 0; }
      html { font-family: "Trebuchet MS", "Segoe UI", Tahoma, sans-serif; background: var(--bg); color: var(--text); }
      body { min-height: 100vh; display: flex; flex-direction: column; overflow-wrap: anywhere; }
      a { color: var(--accent); text-decoration: none; }
      a:hover { text-decoration: underline; }
      a:focus-visible { border-radius: 3px; outline: 3px solid var(--accent); outline-offset: 4px; }
      header {
        border-bottom: 1px solid var(--border);
        padding: 16px 32px;
        display: flex;
        align-items: center;
        justify-content: space-between;
        background: var(--bg2);
      }
      header .logo { font-weight: 700; font-size: 1.1rem; letter-spacing: 0.04em; color: var(--text); }
      header nav { display: flex; flex-wrap: wrap; gap: 12px 24px; justify-content: flex-end; }
      header nav a { color: var(--text-muted); font-size: 0.9rem; }
      header nav a:hover { color: var(--text); text-decoration: none; }
      main { flex: 1; max-width: 860px; margin: 0 auto; padding: 48px 32px; width: 100%; }
      footer {
        border-top: 1px solid var(--border);
        padding: 20px 32px;
        text-align: center;
        font-size: 0.8rem;
        color: var(--text-muted);
      }
      @media (max-width: 560px) {
        header { align-items: stretch; flex-direction: column; gap: 10px; padding: 14px 18px; }
        header .logo { flex: none; }
        header nav { justify-content: flex-start; width: 100%; }
        header nav a { min-height: 32px; padding: 6px 0; }
        main { padding: 34px 18px; }
        footer { padding-inline: 18px; }
      }
    </style>
  </head>
  <body>
    <header>
      <a class="logo" href={`${base}/`}>Keyboard Helper</a>
      <nav aria-label="Primary navigation">
        <a href={`${base}/`}>Home</a>
        <a href={`${base}/setup/`}>Desktop setup</a>
        <a href={`${base}/android/`}>Android</a>
        <a href={`${base}/faq/`}>FAQ</a>
        <a href="https://github.com/maxistar/keyboard_helper" target="_blank" rel="noopener">GitHub</a>
      </nav>
    </header>
    <main>
      <slot />
    </main>
    <footer>
      Keyboard Helper — open source desktop tool and Android companion preview
    </footer>
  </body>
</html>
